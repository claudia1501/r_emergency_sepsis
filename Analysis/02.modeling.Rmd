```{r}
# Crear un dataframe de datos con valores NA en 'age_num' a predecir
df_to_predict <- final_df_order[is.na(final_df_order$age_num), ]

# Utilizar el modelo para predecir los valores faltantes
predictions <- predict(model, newdata = df_to_predict)

# Asignar las predicciones a las filas correspondientes en final_df_order
final_df_order$age_num[is.na(final_df_order$age_num)] <- predictions

```

